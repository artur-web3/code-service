# Telegram API credentials (get them at https://my.telegram.org/apps)
API_ID=your_api_id_here
API_HASH=your_api_hash_here

# Phone number in international format (e.g., +79991234567)
PHONE_NUMBER=your_phone_number_here

# Session name (optional, default 'session')
SESSION_NAME=session

# Chat number that sends the login code (default '42777')
CODE_CHAT_NUMBER=42777

# 2FA password (if two-factor authentication is enabled)
TWO_FA_PASSWORD=your_2fa_password_here

# Webhook URL to send the code via HTTP (for CI/CD)
WEBHOOK_URL=https://your-webhook-url.com/api/code

# Optional: one-time login code from logs/CI to skip waiting for chat
LOGIN_CODE=
